export default class extends Pages {
    title = "Catalogue | NyanScan"

    get raw() {
        return `Catalogue`;
    }

    getHTML(vars) {
        return super.getHTML(vars);
    }

    build(parent, vars) {
        super.build(parent, vars);
    }

    constructor(app) {
        super(app);
    }

}
