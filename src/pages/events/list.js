export default class extends Pages {
	title = "Évènements | NyanScan"
	
	get raw() {
		return `évènement de la liste`;
	}
	
	//TODO to make
	
	getHTML(vars) {
		return super.getHTML(vars);
	}
	
	build(parent, vars) {
		super.build(parent, vars);
	}
	
	constructor(app) {
		super(app);
	}
	
}